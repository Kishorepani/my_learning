#This is what assigned me
def add(x,y)
#if a and y are both integers, then add them
if(type(x)== int and type(y)==int):
return x+y
#otherwise return None
return None

#Tom needs to do this
def mul(x,y)
return x*y
